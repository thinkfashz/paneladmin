import { OmnifixLoading } from "@/fabrick/branding/omnifix-loading";

export default function LoginLoading() {
  return (
    <OmnifixLoading
      label="Abriendo acceso Omnifix"
      description="Preparando la entrada segura del panel con la animación de marca."
    />
  );
}
