# CHANGELOG — Dashboard Default

## Base modular creada

- Se creó carpeta modular `dashboard-default`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Resumen general, métricas, tarjetas y gráficas principales.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
