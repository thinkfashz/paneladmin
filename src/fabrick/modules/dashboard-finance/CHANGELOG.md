# CHANGELOG — Dashboard Finance

## Base modular creada

- Se creó carpeta modular `dashboard-finance`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Mostrar métricas financieras, ingresos, gastos y balance.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
