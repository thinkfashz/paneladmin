# CHANGELOG — Diseño de Marca

## Base modular creada

- Se creó carpeta modular `design`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Editar colores, tipografía, tema visual y preview de marca.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
\n## Migración desde ruta existente\n\n- Se migró la página desde `src/app/admin/diseno` hacia `src/fabrick/modules/design/page.tsx`.\n\n- Se migraron componentes desde `src/app/admin/diseno/_components` hacia `src/fabrick/modules/design/components`.\n