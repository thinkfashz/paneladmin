# CHANGELOG — Analytics Fabrick

## Base modular creada

- Se creó carpeta modular `analytics`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Mostrar métricas, gráficas y proyecciones del negocio.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
\n## Migración desde ruta existente\n\n- Se migró la página desde `src/app/admin/analytics` hacia `src/fabrick/modules/analytics/page.tsx`.\n\n- Se migraron componentes desde `src/app/admin/analytics/_components` hacia `src/fabrick/modules/analytics/components`.\n