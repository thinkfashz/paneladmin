# CHANGELOG — Roles

## Base modular creada

- Se creó carpeta modular `roles`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Roles, permisos y acceso por módulo.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
