# CHANGELOG — CRM Fabrick

## Base modular creada

- Se creó carpeta modular `crm`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Gestionar clientes, prospectos, pipeline y oportunidades comerciales.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
\n## Migración desde ruta existente\n\n- Se migró la página desde `src/app/admin/crm` hacia `src/fabrick/modules/crm/page.tsx`.\n