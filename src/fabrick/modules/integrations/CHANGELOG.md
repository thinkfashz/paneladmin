# CHANGELOG — Integraciones

## Base modular creada

- Se creó carpeta modular `integrations`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Meta, TikTok, Google, WhatsApp, email, automatizaciones y APIs.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
