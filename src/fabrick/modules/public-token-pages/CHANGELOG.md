# CHANGELOG — Páginas Públicas por Token

## Base modular creada

- Se creó carpeta modular `public-token-pages`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Controlar publicación, expiración, verificación y acceso público.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
