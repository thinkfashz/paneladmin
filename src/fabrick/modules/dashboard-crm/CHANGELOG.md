# CHANGELOG — Dashboard CRM

## Base modular creada

- Se creó carpeta modular `dashboard-crm`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Dashboard de ventas, contactos, oportunidades y seguimiento.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
