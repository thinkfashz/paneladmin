# CHANGELOG — Chat

## Base modular creada

- Se creó carpeta modular `chat`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Conversaciones internas o soporte cliente.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
