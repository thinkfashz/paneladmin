# CHANGELOG — Checkout

## Base modular creada

- Se creó carpeta modular `checkout`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Pagos, IVA, ubicación, resumen de compra y confirmación.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
