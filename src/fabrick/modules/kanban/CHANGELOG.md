# CHANGELOG — Kanban

## Base modular creada

- Se creó carpeta modular `kanban`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Tablero visual de tareas, etapas y procesos.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
