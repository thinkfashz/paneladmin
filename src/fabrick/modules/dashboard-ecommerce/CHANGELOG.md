# CHANGELOG — Dashboard E-commerce

## Base modular creada

- Se creó carpeta modular `dashboard-ecommerce`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Ventas, pedidos, productos, carrito y métricas de tienda.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
