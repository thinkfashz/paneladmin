# CHANGELOG — E-commerce Fabrick

## Base modular creada

- Se creó carpeta modular `ecommerce`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Productos, categorías, pedidos, carrito, tienda y stock.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
