# CHANGELOG — Clientes Fabrick

## Base modular creada

- Se creó carpeta modular `customers`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Gestionar clientes, historial y datos comerciales.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
\n## Migración desde ruta existente\n\n- Se migró la página desde `src/app/admin/customers` hacia `src/fabrick/modules/customers/page.tsx`.\n