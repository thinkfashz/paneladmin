# CHANGELOG — Calendar

## Base modular creada

- Se creó carpeta modular `calendar`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Agenda, reuniones, recordatorios y planificación.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
