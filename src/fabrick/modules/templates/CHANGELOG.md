# CHANGELOG — Plantillas HTML

## Base modular creada

- Se creó carpeta modular `templates`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Guardar plantillas por nicho para reutilizarlas en páginas generadas.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
