# CHANGELOG — Invoice

## Base modular creada

- Se creó carpeta modular `invoice`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Facturas, boletas, documentos tributarios y estados de pago.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
