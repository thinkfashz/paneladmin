# CHANGELOG — Admin Hub

## Base modular creada

- Se creó carpeta modular `admin-hub`.
- Se añadió `MODULE.md`.
- Se añadió `nav.ts`.
- Se añadió `types.ts`.
- Se añadió `data.ts`.
- Se añadió estructura `components/`, `actions/`, `services/`.

### Lógica inicial
Servir como índice de módulos y guía de navegación modular.

### Próximo paso
Terminar la funcionalidad real del módulo en un PR dedicado.
